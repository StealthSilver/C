#include <stdio.h>

int main()
{
    int age = 43;
    float pi = 3.14;
    char hashtag = '#';

    printf("%d\n", age);     // %d is for integers
    printf("%f\n", pi);      // %f is for floating-point numbers
    printf("%c\n", hashtag); // %c is for characters
    return 0;
}