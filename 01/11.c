// checking the operations with boolean variables

#include <stdio.h>

int main()
{

    int isSunday = 1;
    int isRaining = 0;

    printf("%d \n", isSunday && isRaining);
    printf("%d \n", isSunday || isRaining);

    return 0;
}